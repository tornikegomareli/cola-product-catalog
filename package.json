{
  "name": "cola-product-catalog",
  "version": "1.0.0",
  "description": "Product image catalog for labeling reference",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "hono": "^4.4.0"
  }
}
